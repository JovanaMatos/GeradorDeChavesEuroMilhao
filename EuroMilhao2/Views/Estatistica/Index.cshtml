@model Estatistica

@{
    ViewData["Title"] = "Index";
    var quantBd = Model.GetNumerosMaisGerados().Select(x => x.Total).FirstOrDefault();



}




<section id="logo">
    <img src="~/img/header.png" alt="logo">
</section>

<h3 class="msg2">5 chaves mais geradas em  @quantBd</h3>

<table class="table table-striped">



    <tr class="table-warning">

        <th>
           Numeros
        </th>
        <th>
            Quantidades extraidas
        </th>
        <th>
            Porcentagem
            
        </th>
       

        @foreach (var item in Model.GetNumerosMaisGerados())
        {
            <tr>

                <td>
                    @Html.DisplayFor(modelItem => item.Number)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NumeroFrequencia)
                </td>
                <td>
                   
                    @Html.DisplayFor(modelItem => item.Porcentagem)%
                </td>
              
              
            </tr>
        }
   
</table>
 <h3 class="msg2">5 chaves menos gerados em  @quantBd</h3>

<table class="table table-striped">



    <tr class="table-warning">

        <th>
           Numeros
        </th>
        <th>
            Quantidades extraidas
        </th>
        <th>
            Porcentagem
            
        </th>
       

        @foreach (var item in Model.GetNumerosMenosGerados())
        {
            <tr>

                <td>
                    @Html.DisplayFor(modelItem => item.Number)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NumeroFrequencia)
                </td>
                <td>
                    
                    @Html.DisplayFor(modelItem => item.Porcentagem)%
                </td>
              
              
            </tr>
        }
   
</table>

 